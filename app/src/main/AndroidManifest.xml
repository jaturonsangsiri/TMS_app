<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Get network permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Get network status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to connect and open usb -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-feature android:name="android.hardware.usb.accessory" />

    <!-- Read file, Write file, create excel file -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- Auto open app when booted -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.RESTART_PACKAGES"/>
    <uses-permission android:name="android.permission.GET_TASKS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TMS"
        tools:targetApi="31">

        <receiver android:name=".services.boot_service.StartOnBootReceiver" android:enabled="true" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <service android:name=".services.boot_service.BootStartService" android:enabled="true" android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:lockTaskMode="always"
            android:screenOrientation="landscape"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TMS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- บอก Android system ว่า “ถ้าเกิดเหตุการณ์ USB_ACCESSORY_ATTACHED ให้เรียกแอปนี้” ตัวดักรับ Intent -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"/>
            </intent-filter>

            <!-- To notified of an attached USB accessory -->
            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/accessory_filter">
            </meta-data>
        </activity>
    </application>

</manifest>